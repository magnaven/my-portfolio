---
phase: 02-hero-routing
plan: "04"
type: execute
wave: 2
depends_on:
  - "02-02"
  - "02-03"
files_modified: []
autonomous: false
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - A11Y-01

must_haves:
  truths:
    - "Visitor sees the full hero as it was designed — animated headline, credential line, CTAs, nav, scroll indicator"
    - "Animation sequence completes within 1.5s"
    - "Both CTAs route to the correct sections"
    - "prefers-reduced-motion renders all content immediately without animation"
    - "Nav transitions from transparent to solid on scroll"
  artifacts:
    - path: "components/HeroSection.tsx"
      provides: "Fully working hero section (verified visually)"
    - path: "components/NavBar.tsx"
      provides: "Fully working sticky nav (verified visually)"
  key_links:
    - from: "HeroSection CTA buttons"
      to: "#work and #contact sections"
      via: "lenis.scrollTo"
      pattern: "scrollTo"
---

<objective>
Visual verification checkpoint: confirm the full hero + nav experience works correctly in the browser before closing Phase 2.

Purpose: Automated tests verify structure and rendering; this checkpoint verifies the visual experience, animation timing, interaction behavior, and A11Y compliance that cannot be captured by vitest.
Output: Human approval that Phase 2 deliverables meet the visual and interaction spec.
</objective>

<execution_context>
@/Users/idadilfertinker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/idadilfertinker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-hero-routing/02-02-SUMMARY.md
@.planning/phases/02-hero-routing/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated suite and dev server smoke check</name>
  <files></files>
  <action>
Run the full test suite to confirm all vitest tests pass:
```
npx vitest run
```

Run TypeScript type check:
```
npx tsc --noEmit
```

Start the dev server for visual inspection:
```
npm run dev
```

Report the test results and confirm the server is running at localhost:3000.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <done>All vitest tests pass green; npx tsc --noEmit exits 0; dev server is running</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and interaction verification</name>
  <files></files>
  <action>
Present the visual verification checklist to the user. The dev server is already running from Task 1. Pause and wait for human confirmation of all 5 steps below before proceeding.
  </action>
  <verify>
    <automated>MANUAL — human approves visual and interaction check</automated>
  </verify>
  <done>Human types "approved" confirming all 5 visual verification steps pass</done>
  <what-built>
    Full hero section and sticky nav:
    - GSAP SplitText character-by-character headline reveal (~0.45s)
    - Credential line fade-up after headline
    - Two CTA buttons fade up as a unit ("Looking for a design partner?" and "Hiring?")
    - Subtle bounce scroll-down indicator (fades out on scroll)
    - Sticky nav that starts transparent and transitions to solid bg-canvas on scroll
    - All animations respect prefers-reduced-motion
  </what-built>
  <how-to-verify>
**Step 1 — Animation sequence:**
Open http://localhost:3000 in a fresh browser tab (no cache). Observe:
- Headline characters animate in one by one, quick and sharp (completes in ~0.45s)
- "own it." appears in terracotta/accent color
- Credential line fades up immediately after headline
- Two CTA buttons fade up together
- Nav fades in (~1.4s after load)
- Chevron bounce indicator is visible at the bottom of the hero
- Total sequence from page load to fully visible: under 1.5s

**Step 2 — CTA routing:**
- Click "Looking for a design partner?" — page should smooth-scroll to the #work section (blank section for now)
- Click "Hiring?" — page should smooth-scroll to the #contact section (blank section for now)
- Both scrolls should use Lenis smooth scrolling (fluid deceleration, not instant jump)

**Step 3 — Nav scroll behavior:**
- Scroll down past the hero — nav should transition from transparent to solid (canvas color) with a smooth CSS transition
- Nav should remain stuck to the top throughout scrolling
- Work/About/Contact links should smooth-scroll to their respective sections

**Step 4 — prefers-reduced-motion (A11Y-01):**
- Open Chrome DevTools → Rendering tab (three-dot menu → More tools → Rendering)
- Under "Emulate CSS media feature", select `prefers-reduced-motion: reduce`
- Reload the page
- Verify: all content (headline, credential line, CTAs, nav) appears immediately at full opacity — no character-by-character reveal, no fades
- Verify: scroll-down indicator is visible but not animating

**Step 5 — Typography check:**
- Headline uses Playfair Display (serif, display font) at large size (~96–128px)
- Credential line and CTA buttons use DM Sans (clean sans-serif)
- "own it." is distinctly terracotta/accent colored
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 steps pass. Describe any issues found (e.g., "animation too slow", "CTA color wrong", "nav doesn't transition") and they will be fixed before closing the phase.</resume-signal>
</task>

</tasks>

<verification>
Phase 2 is complete when:
1. `npx vitest run` — all tests green
2. `npx tsc --noEmit` — zero errors
3. Human checkpoint approved — animation, routing, nav behavior, and A11Y all confirmed
</verification>

<success_criteria>
- Full test suite green (HeroSection + NavBar stubs passing)
- TypeScript zero errors
- Animation sequence visually confirmed: headline → credential → CTAs → nav, under 1.5s total
- "own it." in terracotta accent color confirmed
- CTA scroll routing confirmed (design partner → #work, hiring → #contact)
- Nav transparent → solid transition confirmed
- prefers-reduced-motion: all content immediately visible, no animation confirmed
- Human checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/02-hero-routing/02-04-SUMMARY.md`
</output>
