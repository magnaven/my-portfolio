---
phase: 03-work-section-case-studies
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - components/WorkSection.test.tsx
  - components/CaseStudyChapters.test.tsx
autonomous: true
requirements: [WORK-01, WORK-02, WORK-03, WORK-04]
must_haves:
  truths:
    - "npx vitest run runs without error (tests fail RED as expected for stub stubs)"
    - "WorkSection.test.tsx exists and covers WORK-01 (renders cards with title/category/stat) and WORK-02 (each card links to /work/[slug])"
    - "CaseStudyChapters.test.tsx exists and covers WORK-03 (all four chapters render) and WORK-04 (key decisions render with what/killed/constraint/chosen)"
    - "Both test files mock gsap, gsap/ScrollTrigger, and lenis/react at vi.mock level — identical pattern to HeroSection.test.tsx"
  artifacts:
    - path: "components/WorkSection.test.tsx"
      provides: "Failing test stubs for WORK-01 and WORK-02"
    - path: "components/CaseStudyChapters.test.tsx"
      provides: "Failing test stubs for WORK-03 and WORK-04"
  key_links:
    - from: "components/WorkSection.test.tsx"
      to: "components/WorkSection.tsx"
      via: "dynamic import (same pattern as HeroSection.test.tsx)"
      pattern: "await import.*WorkSection"
    - from: "components/CaseStudyChapters.test.tsx"
      to: "components/CaseStudyChapters.tsx"
      via: "dynamic import"
      pattern: "await import.*CaseStudyChapters"
---

<objective>
Create test scaffolds for Phase 3 components before any implementation exists. Tests run RED immediately, turning GREEN when implementation plans complete.

Purpose: Nyquist compliance — every implementation task in Plans 01–03 references these test files in its automated verify command. Without these files the verify commands fail to run at all, which violates the sampling contract.
Output: Two test files with failing stubs covering all four WORK requirements.
</objective>

<execution_context>
@/Users/idadilfertinker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/idadilfertinker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-work-section-case-studies/03-VALIDATION.md

Reference the established mocking pattern from:
@components/HeroSection.test.tsx

<interfaces>
<!-- Key contracts the executor needs. Do not restructure or deviate from these. -->

From data/case-studies.ts:
```typescript
export interface CaseStudy {
  id: string;
  title: string;
  category: string;
  thumbnailAlt: string;
  problem: string;
  process: string;
  keyDecisions: KeyDecision[];
  outcomes: Outcome[];
  status: "published" | "draft";
}

export interface KeyDecision {
  what: string;
  killed: string;
  constraint: string;
  chosen: string;
}

export interface Outcome {
  metric: string;
  result: string;
}

export const caseStudies: CaseStudy[];
// 3 entries: "aida-ai-b2b-pivot", "connectd-three-sided-platform", "vccp-healthcare-design"
// All status: "published"
// outcomes[0].result for aida: "32% increase post-launch..."
```

Mocking pattern (from HeroSection.test.tsx — replicate verbatim for gsap, add ScrollTrigger):
```typescript
vi.mock("gsap", () => ({
  default: {
    registerPlugin: vi.fn(),
    matchMedia: vi.fn(() => ({
      add: vi.fn((conditions, callback) => {
        callback({ conditions: { reduceMotion: false } });
      }),
    })),
    set: vi.fn(),
    from: vi.fn(),
  },
}));
vi.mock("gsap/ScrollTrigger", () => ({
  default: { create: vi.fn(), refresh: vi.fn() },
}));
vi.mock("lenis/react", () => ({
  useLenis: vi.fn(() => ({ scrollTo: vi.fn() })),
}));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create WorkSection test scaffold (WORK-01, WORK-02)</name>
  <files>components/WorkSection.test.tsx</files>
  <behavior>
    - Test: WorkSection renders a list item for each published case study (3 total)
    - Test: Each rendered card contains the case study title text
    - Test: Each rendered card contains the category text
    - Test: Each rendered card contains the first outcome stat result text
    - Test: Each card contains a link (anchor or Next.js Link) pointing to /work/[slug] where slug is the case study id
  </behavior>
  <action>
    Create components/WorkSection.test.tsx. These tests MUST fail (RED) because WorkSection.tsx does not exist yet.

    Mock setup at the top of the file — copy the gsap mock from HeroSection.test.tsx, then add gsap/ScrollTrigger and lenis/react mocks. Use vi.mock("next/link", ...) to render a plain anchor so href is inspectable in jsdom.

    Mock next/link as:
    ```typescript
    vi.mock("next/link", () => ({
      default: ({ href, children, ...props }: { href: string; children: React.ReactNode; [key: string]: unknown }) =>
        <a href={href} {...props}>{children}</a>,
    }));
    ```

    Test file structure:
    - describe("WorkSection — WORK-01"): tests for title, category, outcome stat rendering
    - describe("WorkSection — WORK-02"): tests for card link href values

    Import WorkSection via dynamic import inside each test (same pattern as HeroSection.test.tsx):
    ```typescript
    const { WorkSection } = await import("../components/WorkSection");
    ```

    Use the real caseStudies array imported from "@/data/case-studies" to assert against actual data values, not hardcoded strings. This ensures tests stay in sync with content changes.
  </action>
  <verify>
    <automated>cd /Users/idadilfertinker/Desktop/ida-portfolio/ida-portfolio && npx vitest run components/WorkSection.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>File exists. Tests run. Tests fail with "Cannot find module '../components/WorkSection'" — RED as expected. No syntax errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create CaseStudyChapters test scaffold (WORK-03, WORK-04)</name>
  <files>components/CaseStudyChapters.test.tsx</files>
  <behavior>
    - Test: CaseStudyChapters renders a section/div for each of the four chapters: Problem, Process, Key Decisions, Outcomes
    - Test: Problem chapter contains the study.problem text
    - Test: Process chapter contains the study.process text
    - Test: Key Decisions chapter renders one card per keyDecision entry (2 entries in AIDA AI study)
    - Test: Each decision card contains the "what" field text
    - Test: Each decision card contains the "killed" field text
    - Test: Each decision card contains the "constraint" field text
    - Test: Each decision card contains the "chosen" field text
    - Test: With reduceMotion=true in matchMedia mock, all chapter content renders at autoAlpha:1 (content is present, not hidden)
  </behavior>
  <action>
    Create components/CaseStudyChapters.test.tsx. These tests MUST fail (RED) because CaseStudyChapters.tsx does not exist yet.

    Mocks required: gsap (with matchMedia that accepts a reduceMotion condition), gsap/ScrollTrigger, lenis/react.

    For the reduceMotion test, override the matchMedia mock inside the test to pass reduceMotion: true:
    ```typescript
    vi.mocked(gsap.matchMedia).mockImplementationOnce(() => ({
      add: vi.fn((conditions, callback) => {
        callback({ conditions: { reduceMotion: true } });
      }),
    }));
    ```

    Use the first caseStudy from the real caseStudies array (id: "aida-ai-b2b-pivot") as the test fixture. Import it:
    ```typescript
    import { caseStudies } from "@/data/case-studies";
    const testStudy = caseStudies[0]; // aida-ai-b2b-pivot, 2 keyDecisions
    ```

    Render: `<CaseStudyChapters study={testStudy} />`

    Test structure:
    - describe("CaseStudyChapters — WORK-03"): four chapters render, content present
    - describe("CaseStudyChapters — WORK-04"): key decisions entries render with all four fields
    - describe("CaseStudyChapters — A11Y-01 / reduceMotion"): content visible with reduced motion
  </action>
  <verify>
    <automated>cd /Users/idadilfertinker/Desktop/ida-portfolio/ida-portfolio && npx vitest run components/CaseStudyChapters.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>File exists. Tests run. Tests fail with "Cannot find module '../components/CaseStudyChapters'" — RED as expected. No syntax errors.</done>
</task>

</tasks>

<verification>
Both test files exist and produce RED failures (module not found), not syntax errors or import errors. Run:
```
cd /Users/idadilfertinker/Desktop/ida-portfolio/ida-portfolio && npx vitest run
```
The full suite should report existing HeroSection and NavBar tests as passing, plus the two new test files as failing with module-not-found errors.
</verification>

<success_criteria>
- components/WorkSection.test.tsx exists, runs, fails RED
- components/CaseStudyChapters.test.tsx exists, runs, fails RED
- Existing passing tests remain GREEN (no regressions)
- All GSAP/Lenis mocks use vi.mock at file scope per established pattern
</success_criteria>

<output>
After completion, create `.planning/phases/03-work-section-case-studies/03-00-SUMMARY.md`
</output>
