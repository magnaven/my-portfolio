---
phase: 04-about-contact-launch
plan: "01"
type: execute
wave: 2
depends_on:
  - "04-00"
files_modified:
  - components/AboutSection.tsx
  - components/AboutSection.test.tsx
autonomous: true
requirements:
  - ABUT-01
  - ABUT-02

must_haves:
  truths:
    - "Visitor sees the About section label 'About' in small uppercase (matching WorkSection pattern)"
    - "Visitor reads all 3 narrative paragraphs from content/about.ts in their own voice"
    - "Visitor can click 'Download CV' to open the PDF in a new tab"
    - "About section fades up on scroll with prefers-reduced-motion guard"
  artifacts:
    - path: "components/AboutSection.tsx"
      provides: "About section component"
      exports: ["AboutSection"]
      min_lines: 50
  key_links:
    - from: "components/AboutSection.tsx"
      to: "content/about.ts"
      via: "import aboutContent"
      pattern: "import.*aboutContent.*from.*content/about"
    - from: "components/AboutSection.tsx"
      to: "gsap/ScrollTrigger"
      via: "useGSAP + matchMedia reduceMotion guard"
      pattern: "gsap.matchMedia"
---

<objective>
Build AboutSection component: founder narrative, CV download link, scroll-triggered fade-up animation.

Purpose: Delivers ABUT-01 (founder lens narrative) and ABUT-02 (CV download). Mirrors WorkSection pattern exactly — section label, container sizing, GSAP matchMedia guard.
Output: components/AboutSection.tsx with tests passing GREEN.
</objective>

<execution_context>
@/Users/idadilfertinker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/idadilfertinker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-about-contact-launch/04-CONTEXT.md
@.planning/phases/04-about-contact-launch/04-RESEARCH.md
@.planning/phases/04-about-contact-launch/04-00-SUMMARY.md

<interfaces>
<!-- Replicate WorkSection.tsx pattern exactly. Key contracts below. -->

From components/WorkSection.tsx (section label, container, GSAP animation — copy this structure):
```typescript
"use client";
import { useRef } from "react";
import { useGSAP } from "@gsap/react";
import gsap from "gsap";
import ScrollTrigger from "gsap/ScrollTrigger";
gsap.registerPlugin(ScrollTrigger);
// Section label: font-sans text-sm uppercase tracking-widest text-ink/40 mb-16
// Container: max-w-5xl mx-auto px-8
// Animation: gsap.from(".about-content", { autoAlpha:0, y:32, duration:0.5, ease:"power2.out", scrollTrigger: { trigger:"#about", start:"top 75%" } })
// reduceMotion: gsap.set(".about-content", { autoAlpha:1, y:0 })
```

From content/about.ts (source of truth for narrative — import directly):
```typescript
export const aboutContent = {
  narrative: [
    "I'm a Lead Product Designer who thinks like a founder...",
    "The work I'm proudest of is the work that was hardest to explain...",
    "I take on consulting engagements with founding teams...",
  ],
} as const;
```

From components/HeroSection.tsx (ghost button style for CV download):
```typescript
// Ghost button: border border-accent text-accent font-sans px-6 py-3 rounded-sm hover:bg-accent/10 transition-colors inline-block
```

From components/AboutSection.test.tsx (test contracts that must pass GREEN):
- renders all 3 narrative paragraphs from aboutContent.narrative
- renders a link with href="/ida-dilfer-tinker-cv.pdf" and target="_blank"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build AboutSection component (GREEN)</name>
  <files>components/AboutSection.tsx</files>
  <behavior>
    - Renders all 3 paragraphs from aboutContent.narrative (text must be in DOM)
    - Renders an anchor tag with href="/ida-dilfer-tinker-cv.pdf" and target="_blank" and rel="noopener noreferrer"
    - Anchor text is "Download CV"
    - Section id="about" with py-24 bg-canvas
    - Section label "About" matching WorkSection pattern: font-sans text-sm uppercase tracking-widest text-ink/40 mb-16
    - Container div: max-w-5xl mx-auto px-8
    - Content wrapper has className="about-content" (targeted by GSAP)
    - Paragraphs use font-display text-xl text-ink/80 mb-6 leading-relaxed
    - GSAP fade-up: autoAlpha:0 y:32 duration:0.5 ease:power2.out scrollTrigger trigger:#about start:top 75%
    - reduceMotion guard: gsap.set(".about-content", { autoAlpha:1, y:0 }) when reduceMotion condition true
    - Imports: useRef, useGSAP, gsap, ScrollTrigger, aboutContent
    - Top of file: "use client" directive
    - gsap.registerPlugin(ScrollTrigger) at module scope (mirrors WorkSection exactly — not optional even though ScrollTrigger is already registered in SmoothScrollProvider)
  </behavior>
  <action>
    Create components/AboutSection.tsx as a "use client" component.

    Structure mirrors WorkSection.tsx exactly. Key differences:
    - Target class is .about-content (not .case-study-card)
    - Renders narrative paragraphs from aboutContent.narrative array (map with key={i})
    - Renders "Download CV" anchor below the narrative — use ghost button style (border border-accent text-accent font-sans px-6 py-3 rounded-sm hover:bg-accent/10 transition-colors inline-block mt-4) with href="/ida-dilfer-tinker-cv.pdf" target="_blank" rel="noopener noreferrer"
    - Section id="about" (not "work")

    Animation timing at Claude's discretion — use 0.5s duration, power2.out ease, same as WorkSection. The stagger from WorkSection is not needed here (single content block).

    After writing the component, run the test to confirm GREEN:
    npx vitest run components/AboutSection.test.tsx --reporter=verbose

    All 3 tests must pass before committing.
    Commit message: feat(04-01): build AboutSection with narrative and CV download link
  </action>
  <verify>
    <automated>npx vitest run components/AboutSection.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    - components/AboutSection.tsx exists and exports AboutSection
    - All 3 AboutSection tests pass (ABUT-01 narrative paragraphs x2, ABUT-02 CV link)
    - npx vitest run returns 0 failures for this file
  </done>
</task>

</tasks>

<verification>
npx vitest run components/AboutSection.test.tsx --reporter=verbose

Expected: 3 tests pass. ABUT-01 (narrative text present), ABUT-02 (CV link with correct href and target).
</verification>

<success_criteria>
- components/AboutSection.tsx exists, exports AboutSection
- All 3 AboutSection tests GREEN
- Component uses aboutContent.narrative — no hardcoded prose
- CV download link: href="/ida-dilfer-tinker-cv.pdf" target="_blank" rel="noopener noreferrer"
- GSAP animation mirrors WorkSection pattern with .about-content target class
- Commit made: feat(04-01): build AboutSection with narrative and CV download link
</success_criteria>

<output>
After completion, create `.planning/phases/04-about-contact-launch/04-01-SUMMARY.md`
</output>
