---
phase: 04-about-contact-launch
plan: "03"
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - app/page.tsx
  - app/layout.tsx
autonomous: true
requirements:
  - ABUT-01
  - ABUT-02
  - CONT-01
  - CONT-02

must_haves:
  truths:
    - "Homepage renders AboutSection where the min-h-screen placeholder was"
    - "Homepage renders ContactSection where the min-h-screen placeholder was"
    - "OG tags appear in page source: og:title, og:description, og:type"
    - "Full test suite passes with no regressions"
  artifacts:
    - path: "app/page.tsx"
      provides: "Homepage with AboutSection and ContactSection wired in"
      contains: "AboutSection, ContactSection"
    - path: "app/layout.tsx"
      provides: "Extended metadata with openGraph fields"
      contains: "openGraph"
  key_links:
    - from: "app/page.tsx"
      to: "components/AboutSection.tsx"
      via: "import AboutSection"
      pattern: "import.*AboutSection"
    - from: "app/page.tsx"
      to: "components/ContactSection.tsx"
      via: "import ContactSection"
      pattern: "import.*ContactSection"
    - from: "app/layout.tsx"
      to: "Next.js Metadata API"
      via: "export const metadata"
      pattern: "openGraph"
---

<objective>
Wire AboutSection and ContactSection into app/page.tsx, replacing placeholder stubs. Extend layout.tsx metadata with openGraph fields for launch.

Purpose: All four Phase 4 requirements become reachable on the live homepage. OG tags make the site shareable. This is the wiring plan — components already exist and test GREEN, this connects them to the page.
Output: Updated page.tsx and layout.tsx.
</objective>

<execution_context>
@/Users/idadilfertinker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/idadilfertinker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-about-contact-launch/04-CONTEXT.md
@.planning/phases/04-about-contact-launch/04-01-SUMMARY.md
@.planning/phases/04-about-contact-launch/04-02-SUMMARY.md

<interfaces>
<!-- Current state of the two files being updated. -->

Current app/page.tsx:
```typescript
import { HeroSection } from "@/components/HeroSection";
import { WorkSection } from "@/components/WorkSection";

export default function Home() {
  return (
    <main>
      <HeroSection />
      <WorkSection />
      {/* Section anchors — empty placeholders for Phase 4 content */}
      <section id="about" className="min-h-screen bg-canvas" />
      <section id="contact" className="min-h-screen bg-canvas" />
    </main>
  );
}
```

Current app/layout.tsx metadata export (partial — needs openGraph added):
```typescript
export const metadata: Metadata = {
  title: "Ida Dilfer Tinker — Product Designer",
  description: "Portfolio of Ida Dilfer Tinker, Lead Product Designer at Magna Ventures and AIDA AI.",
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace placeholder stubs in app/page.tsx</name>
  <files>app/page.tsx</files>
  <action>
    Replace the two placeholder section elements in app/page.tsx with the real components.

    Replace:
    ```tsx
    {/* Section anchors — empty placeholders for Phase 4 content */}
    <section id="about" className="min-h-screen bg-canvas" />
    <section id="contact" className="min-h-screen bg-canvas" />
    ```

    With:
    ```tsx
    <AboutSection />
    <ContactSection />
    ```

    Add the two imports at the top of the file:
    ```typescript
    import { AboutSection } from "@/components/AboutSection";
    import { ContactSection } from "@/components/ContactSection";
    ```

    Keep the existing HeroSection and WorkSection imports unchanged.

    The section id="about" and id="contact" attributes are now owned by the components themselves (AboutSection renders <section id="about"> and ContactSection renders <section id="contact">) — the Lenis scroll targets already set up in NavBar will continue to work.

    Verify TypeScript after edit:
    npx tsc --noEmit 2>&1 | head -20

    Commit message: feat(04-03): wire AboutSection and ContactSection into homepage
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    app/page.tsx imports and renders AboutSection and ContactSection. No TypeScript errors. The placeholder sections are gone.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend metadata in app/layout.tsx with openGraph fields</name>
  <files>app/layout.tsx</files>
  <action>
    Extend the existing metadata export in app/layout.tsx to add openGraph fields and update the description.

    Replace the current metadata export:
    ```typescript
    export const metadata: Metadata = {
      title: "Ida Dilfer Tinker — Product Designer",
      description: "Portfolio of Ida Dilfer Tinker, Lead Product Designer at Magna Ventures and AIDA AI.",
    };
    ```

    With:
    ```typescript
    export const metadata: Metadata = {
      title: "Ida Dilfer Tinker — Product Designer",
      description: "Lead Product Designer with a founder's lens. 12 years building products at Magna Ventures, AIDA AI, and beyond.",
      openGraph: {
        title: "Ida Dilfer Tinker — Product Designer",
        description: "Lead Product Designer with a founder's lens. 12 years building products at Magna Ventures, AIDA AI, and beyond.",
        type: "website",
      },
    };
    ```

    IMPORTANT: Only extend the existing export — do NOT create a new metadata export in page.tsx. Duplicate metadata exports cause Next.js to merge/override fields unpredictably (per research pitfall 5).

    No OG image field — plain text OG data is the v1 decision (per 04-CONTEXT.md). Image generation is v2.

    Favicon is already present at app/favicon.ico — no changes needed there.

    Run full test suite to check for regressions:
    npx vitest run --reporter=verbose

    Commit message: feat(04-03): add openGraph metadata to layout.tsx for launch
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    app/layout.tsx metadata export includes openGraph.title, openGraph.description, openGraph.type="website". Full test suite passes (0 failures). Updated description is consistent with OG description.
  </done>
</task>

</tasks>

<verification>
npx vitest run --reporter=verbose
npx tsc --noEmit

Expected: All tests pass (0 failures). TypeScript clean. No regressions from wiring.
</verification>

<success_criteria>
- app/page.tsx renders AboutSection and ContactSection (placeholder stubs removed)
- app/layout.tsx metadata has openGraph.title, openGraph.description, openGraph.type
- npx vitest run returns 0 failures across all test files
- npx tsc --noEmit returns no errors
- Two commits made: wiring commit + metadata commit
</success_criteria>

<output>
After completion, create `.planning/phases/04-about-contact-launch/04-03-SUMMARY.md`
</output>
