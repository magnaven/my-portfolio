---
phase: 04-about-contact-launch
plan: "04"
type: execute
wave: 4
depends_on:
  - "04-03"
files_modified: []
autonomous: false
requirements:
  - ABUT-01
  - ABUT-02
  - CONT-01
  - CONT-02

user_setup:
  - service: cal.com
    why: "Cal.com popup booking button needs a real event link before it works"
    env_vars: []
    dashboard_config:
      - task: "Create a Cal.com account and an event type (e.g. '30 min intro call')"
        location: "cal.com/getting-started"
      - task: "Copy the event URL slug (e.g. 'ida/30min') and paste it into content/contact.ts calLink field"
        location: "content/contact.ts line 3"
  - service: linkedin
    why: "LinkedIn URL is a placeholder in content/contact.ts"
    env_vars: []
    dashboard_config:
      - task: "Replace the placeholder LinkedIn URL in content/contact.ts with Ida's actual profile URL"
        location: "content/contact.ts line 6"
  - service: resume-pdf
    why: "CV download link 404s until the file is placed"
    env_vars: []
    dashboard_config:
      - task: "Place Ida's CV PDF at public/ida-dilfer-tinker-cv.pdf"
        location: "public/ida-dilfer-tinker-cv.pdf"

must_haves:
  truths:
    - "About section is visible, readable, and fades up as the user scrolls to it"
    - "Download CV link opens the PDF in a new tab (or 404s only if file not yet placed)"
    - "Contact section shows two columns side by side on desktop"
    - "'Book a call' button triggers the Cal.com modal (or 404s only if placeholder link not replaced)"
    - "'View LinkedIn' opens a new tab to LinkedIn (or placeholder URL if not replaced)"
    - "prefers-reduced-motion: About animation does not play when motion is reduced"
    - "Full site conversion flow works: land on hero, scroll to work, scroll to about, reach contact"
  artifacts:
    - path: "components/AboutSection.tsx"
      provides: "About section rendered on homepage"
    - path: "components/ContactSection.tsx"
      provides: "Contact section rendered on homepage"
  key_links: []
---

<objective>
Visual verification: confirm About and Contact sections render correctly in the browser, the full page conversion flow works end-to-end, and prefers-reduced-motion is respected.

Purpose: Human eye check — the automated tests confirm structure, not visual correctness or interactive behavior. This checkpoint gates launch readiness.
Output: Visual sign-off. User confirms site is ready to share (once placeholder URLs are replaced).
</objective>

<execution_context>
@/Users/idadilfertinker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/idadilfertinker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-about-contact-launch/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and run full test suite</name>
  <files></files>
  <action>
    Run the full test suite to confirm all tests pass before asking the user to verify:
    npx vitest run --reporter=verbose

    Then start the dev server:
    npm run dev

    Confirm the server starts without errors. Note the URL (typically http://localhost:3000).

    Do NOT proceed to the checkpoint until tests are green and the dev server is running.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>All tests pass. Dev server running at http://localhost:3000.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification — About, Contact, full page flow</name>
  <action>Human visual inspection of the complete homepage at http://localhost:3000.</action>
  <what-built>
    Phase 4 complete: AboutSection and ContactSection wired into the homepage at http://localhost:3000.

    What to look for:
    - About section: founder narrative in 3 paragraphs, "Download CV" ghost button at bottom
    - Contact section: two columns ("Looking for a design partner?" left, "Hiring?" right), "Book a call" button, "View LinkedIn" link
    - Full page scroll: hero → work → about → contact — all sections reachable via Lenis smooth scroll
    - Nav anchor links: clicking "About" and "Contact" in the nav bar scrolls to correct sections
    - OG tags: right-click page → View Page Source → search for "og:title" to confirm it appears
    - prefers-reduced-motion: enable in OS (System Preferences > Accessibility > Motion > Reduce Motion), reload page — About section should appear without animation, work section and hero should also skip their animations

    BEFORE-LAUNCH placeholders (expected at this point — not defects):
    - "Book a call" button may open a Cal.com 404 page — this is expected until Ida sets up her Cal.com account and updates content/contact.ts calLink
    - "View LinkedIn" may go to a placeholder URL — expected until Ida updates content/contact.ts linkedInUrl
    - "Download CV" may 404 — expected until Ida places public/ida-dilfer-tinker-cv.pdf
  </what-built>
  <how-to-verify>
    1. Visit http://localhost:3000
    2. Scroll slowly from top to bottom — confirm smooth scroll and that About section fades in
    3. Verify About section: 3 paragraphs of narrative are readable, "Download CV" button is visible and styled with a border
    4. Verify Contact section: two side-by-side columns on desktop, both with appropriate headings and styled buttons
    5. Click "Book a call" — Cal.com modal should open (or show 404 if placeholder link — acceptable pre-launch)
    6. Click "View LinkedIn" — new tab should open (acceptable if placeholder URL)
    7. Check nav links: click "About" and "Contact" — confirm Lenis smooth-scrolls to the correct section
    8. Source check: View Page Source, search for "og:title" — confirm it appears with "Ida Dilfer Tinker"
    9. Optional — test reduced motion: macOS System Preferences > Accessibility > Display > Reduce Motion, reload page, scroll to About — it should appear without animation
  </how-to-verify>
  <resume-signal>
    If everything looks correct: type "approved"
    If there are visual issues: describe what's wrong and Claude will fix before launch
  </resume-signal>
  <verify>
    <automated>MISSING — visual verification only; no automated command for rendering fidelity</automated>
  </verify>
  <done>Human approves visual output. Both sections look correct and consistent with the existing site aesthetic.</done>
</task>

</tasks>

<verification>
Human visual inspection at http://localhost:3000. All sections visible. Scroll flow works. Styling consistent with existing sections.
</verification>

<success_criteria>
- About section renders founder narrative (3 paragraphs) and Download CV button
- Contact section renders two-column layout with Book a call and View LinkedIn
- Lenis nav scroll targets work for #about and #contact
- OG tags confirmed present in page source
- prefers-reduced-motion: About section animation skipped
- Human approves visual output
- Phase 4 considered complete after approval
</success_criteria>

<output>
After completion, create `.planning/phases/04-about-contact-launch/04-04-SUMMARY.md`
</output>
